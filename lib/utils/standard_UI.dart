import 'package:flutter/material.dart';

class UI {
  static const Color backgroundApp = Color.fromARGB(255, 255, 92, 52);
  static const Color wordTile = Colors.white;
  static const double wordTileSize = 30;
}
